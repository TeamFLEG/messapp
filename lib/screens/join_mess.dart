import 'package:flutter/material.dart';


class JoinMess extends StatelessWidget {
  const JoinMess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("Join Mess Page"),
    );
  }
}